import React from 'react';

const Notification = () => {
    return (
        <div>
            <h1>There are No Notification available now...</h1>
        </div>
    );
};

export default Notification;